meta {
  name: Update
  type: http
  seq: 4
}

put {
  url: {{url}}/media_users/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "status": "done",
    "background": "test"
  }
}

settings {
  encodeUrl: true
}
