meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{url}}/genres
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "name": "Genre 1"
  }
}

settings {
  encodeUrl: true
}
