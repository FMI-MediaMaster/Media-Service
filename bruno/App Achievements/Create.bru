meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{url}}/app_achievements
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "name": "Achievement 1",
    "description": "Add 1 items to your libraries.",
    "xp": 20
  }
}

settings {
  encodeUrl: true
}
