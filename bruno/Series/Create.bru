meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{url}}/series
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "name": "Red rising"
  }
}

settings {
  encodeUrl: true
}
