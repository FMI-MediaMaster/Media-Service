meta {
  name: Update
  type: http
  seq: 4
}

put {
  url: {{url}}/wishlist/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "background": "test"
  }
}

settings {
  encodeUrl: true
}
