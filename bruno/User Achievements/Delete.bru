meta {
  name: Delete
  type: http
  seq: 6
}

delete {
  url: {{url}}/user_achievements/{{validId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

settings {
  encodeUrl: true
}
