meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{url}}/sources
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "name": "Steam"
  }
}

settings {
  encodeUrl: true
}
